import { defaultTokens } from "@kiwicom/orbit-design-tokens"

const theme = {
  yourCustomTheme: {
    spacing: '8px'
  },
  orbit: defaultTokens,
  rtl: false,
  transitions: false,
  lockScrolling: false,
};

export default theme;
